#!/usr/bin/env node

const program = require('commander');

const { initializeCommand } = require('../lib/core/initializer');
initializeCommand(program, 'npm');

program
    .command('download', 'Download package tarballs').alias('d')
    .command('publish', 'Publish package tarballs').alias('p')
    .command('copy', 'Download packages from one registry and publish to another').alias('c')
    .parse(process.argv);
