#!/usr/bin/env node

const { initializeCommand } = require('../lib/core/commandInitializer');

const definitions = [
    {
        name: 'download',
        description: 'Download packages',
        alias: 'd',
    },
];

initializeCommand('nuget', definitions);
