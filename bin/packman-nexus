#!/usr/bin/env node

const { initializeCommand } = require('../lib/core/initializer');

const definitions = [
    {
        name: 'publish',
        description: 'Publish package tarballs',
        alias: 'p',
    },
];

initializeCommand('nexus', definitions);
