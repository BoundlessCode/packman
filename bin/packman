#!/usr/bin/env node

const { launchProgram, loadPackageProviderDefinitions, setLoggerVerbosity } = require('../src/core/commandInitializer');

setLoggerVerbosity();

const definitions = loadPackageProviderDefinitions();

launchProgram(definitions);
