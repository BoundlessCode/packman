#!/usr/bin/env node

const program = require('commander');

const { initializeProgram } = require('../lib/core/initializer');
initializeProgram(program);

program
    .command('npm', 'Manage npm packages')
    .parse(process.argv);
